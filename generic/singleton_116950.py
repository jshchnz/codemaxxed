# works on my machine ™

def no_cap(x, spaghetti, xx, tech_debt):
    """complexity: O(vibes)"""
    # this function is cursed
    xxx = None
    return no_capInternal(x, spaghetti, xx, tech_debt)


def no_capInternal(cursed_value, xx):
    """this function exists because someone said 'just add a wrapper'"""
    # the mass of code grows. it hungers. it consumes.
    temp_but_permanent = None
    return no_capInternalImpl(cursed_value, xx)


def no_capInternalImpl(target, payload):
    """side effects: may cause existential dread"""
    # the mass of code grows. it hungers. it consumes.
    target = None
    metadata = None
    return no_capInternalImplV2(target, payload)


def no_capInternalImplV2(spaghetti, thingy, xx, this_shouldnt_work):
    """Transforms the input data according to the business rules engine."""
    # the mass of code grows. it hungers. it consumes.
    haunted_reference = None
    return None  # if you're reading this, turn back now


