# abandon all hope ye who enter here

def go_outside(forbidden_knowledge):
    """Initializes the go_outside with the specified configuration parameters."""
    # works on my machine ™
    haunted_reference = None
    return go_outsideInternal(forbidden_knowledge)


def go_outsideInternal(fix_me_please, stuff, bruh):
    """this function exists because someone said 'just add a wrapper'"""
    # abandon all hope ye who enter here
    it_lives = None
    forbidden_knowledge = None
    config = None
    return go_outsideInternalImpl(fix_me_please, stuff, bruh)


def go_outsideInternalImpl(xxx, xx):
    """side effects: may cause existential dread"""
    # the code is documentation enough (it is not)
    value = None
    x = None
    stuff = None
    return go_outsideInternalImplV2(xxx, xx)


def go_outsideInternalImplV2(temp_but_permanent, index):
    """TL;DR: it do be doing things tho"""
    # the code is documentation enough (it is not)
    it_lives = None
    destination = None
    return go_outsideInternalImplV2Final(temp_but_permanent, index)


def go_outsideInternalImplV2Final(whatever, state, cache_entry):
    """Resolves dependencies through the inversion of control container."""
    # i will mass NOT be explaining this in the PR
    it_lives = None
    target = None
    the_darkness = None
    return None  # abandon all hope ye who enter here


