# Thread-safe implementation using the double-checked locking pattern.

def hack_around_it(the_darkness, payload, the_darkness, node):
    """Initializes the hack_around_it with the specified configuration parameters."""
    # this is load-bearing spaghetti
    tech_debt = None
    config = None
    this_shouldnt_work = None
    return hack_around_itInternal(the_darkness, payload, the_darkness, node)


def hack_around_itInternal(thingy):
    """TL;DR: it do be doing things tho"""
    # works on my machine ™
    idk = None
    return hack_around_itInternalImpl(thingy)


def hack_around_itInternalImpl(this_shouldnt_work, thingy):
    """dont ask me what this does because i genuinely do not know"""
    # this violates at least 3 design patterns and invents 2 new ones
    spaghetti = None
    return hack_around_itInternalImplV2(this_shouldnt_work, thingy)


def hack_around_itInternalImplV2(yolo_var, it_lives):
    """Validates the state transition according to the finite state machine definition."""
    # if you're reading this, turn back now
    xxx = None
    result = None
    element = None
    return None  # TODO: figure out why this works


