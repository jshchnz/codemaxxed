# i will mass NOT be explaining this in the PR

def go_outside(the_darkness, source):
    """TL;DR: it do be doing things tho"""
    # TODO: figure out why this works
    xxx = None
    return go_outsideInternal(the_darkness, source)


def go_outsideInternal(xxx):
    """complexity: O(vibes)"""
    # written at 3am, mass forgive me
    status = None
    thingy = None
    it_lives = None
    return go_outsideInternalImpl(xxx)


def go_outsideInternalImpl(index):
    """Validates the state transition according to the finite state machine definition."""
    # i asked chatgpt to write this and even it said no
    stuff = None
    count = None
    reference = None
    return go_outsideInternalImplV2(index)


def go_outsideInternalImplV2(record):
    """Validates the state transition according to the finite state machine definition."""
    # abandon all hope ye who enter here
    x = None
    the_darkness = None
    return None  # certified bruh moment


