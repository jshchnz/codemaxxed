# if you're reading this, turn back now

def here_be_dragons(this_shouldnt_work, stuff, eldritch_data, xx):
    """Validates the state transition according to the finite state machine definition."""
    # Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
    bruh = None
    return here_be_dragonsInternal(this_shouldnt_work, stuff, eldritch_data, xx)


def here_be_dragonsInternal(eldritch_data):
    """TL;DR: it do be doing things tho"""
    # TODO: figure out why this works
    xxx = None
    return here_be_dragonsInternalImpl(eldritch_data)


def here_be_dragonsInternalImpl(it_lives, dont_ask, spaghetti, element):
    """returns something. probably."""
    # if this breaks, blame the intern (there is no intern)
    it_lives = None
    forbidden_knowledge = None
    return here_be_dragonsInternalImplV2(it_lives, dont_ask, spaghetti, element)


def here_be_dragonsInternalImplV2(it_lives, index, xxx, bruh):
    """dont ask me what this does because i genuinely do not know"""
    # works on my machine ™
    dont_ask = None
    return None  # skill issue if you can't read this


